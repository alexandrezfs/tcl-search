/** @jsx React.DOM */

var React = require('react');

module.exports = TclApp = React.createClass({
    // Render the component
    render: function(){

        return (
            <div className="tweets-app">
                blabla
            </div>
        )

    }
});